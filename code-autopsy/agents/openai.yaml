interface:
  display_name: "CodeAutopsy"
  short_description: "X-Ray onboarding visualizer"
  default_prompt: "Use $code-autopsy to run X-Ray mode on a local repo and generate diagrams, onboarding docs, an interactive viewer, and PNG snapshots."
